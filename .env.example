# Strict comma-separated parser names. Empty, unknown, and duplicate names are rejected.
# Currently supported: pumpfun
# Multi-parser format once PumpSwap is supported: PARSERS=pumpfun,pumpswap
PARSERS=pumpfun
